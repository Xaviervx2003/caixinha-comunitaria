import { describe, test, expect } from "vitest"; import Decimal from "decimal.js"; import { calcMonthlyPayment, safeNumber, calculateProgress, calculateMonthlyTotal, calcLateMonthlyPayment, isLatePayment, splitPaymentByRole, calculateCollectionsFromTransactions } from "./finance"; describe("calcMonthlyPayment", () => { test("membro sem divida paga R$200", () => { expect(calcMonthlyPayment(new Decimal("0"), "member").total.toFixed(2)).toBe("200.00"); }); test("membro R$1000 paga R$300", () => { expect(calcMonthlyPayment(new Decimal("1000"), "member").total.toFixed(2)).toBe("300.00"); }); test("externo nao paga cota", () => { expect(calcMonthlyPayment(new Decimal("1000"), "external").quota.toFixed(2)).toBe("0.00"); }); }); describe("calcLateMonthlyPayment", () => { test("atraso membro R$206", () => { expect(calcLateMonthlyPayment(new Decimal("0"), "member").total.toFixed(2)).toBe("206.00"); }); test("atraso externo so juros", () => { expect(calcLateMonthlyPayment(new Decimal("1000"), "external").lateFee.toFixed(2)).toBe("0.00"); }); }); describe("isLatePayment", () => { test("no prazo false", () => { expect(isLatePayment("2025-01", new Date(2025,1,4), 5)).toBe(false); }); test("atrasado true", () => { expect(isLatePayment("2025-01", new Date(2025,1,6), 5)).toBe(true); }); }); describe("splitPaymentByRole", () => { test("membro R$300 split", () => { const r = splitPaymentByRole(300,"member"); expect(r.quota).toBe(200); expect(r.interest).toBe(100); }); test("externo tudo interest", () => { expect(splitPaymentByRole(150,"external").quota).toBe(0); }); }); describe("calculateCollectionsFromTransactions", () => { test("payment membro split", () => { const r = calculateCollectionsFromTransactions([{participantId:1,type:"payment",amount:"300"}],{1:"member"}); expect(r.totalQuotas).toBe(200); }); test("amortizacao no totalFees", () => { const r = calculateCollectionsFromTransactions([{participantId:1,type:"amortization",amount:"500"}],{1:"member"}); expect(r.totalFees).toBe(500); expect(r.totalInterest).toBe(0); }); }); describe("safeNumber", () => { test("string", () => expect(safeNumber("100")).toBe(100)); test("null zero", () => expect(safeNumber(null)).toBe(0)); test("invalido zero", () => expect(safeNumber("abc")).toBe(0)); }); describe("calculateProgress", () => { test("pago 100%", () => expect(calculateProgress(1000,0)).toBe(100)); test("metade 50%", () => expect(calculateProgress(1000,500)).toBe(50)); }); describe("calculateMonthlyTotal", () => { test("membro sem divida R$200", () => expect(calculateMonthlyTotal("0","member")).toBe(200)); test("externo R$1000 R$100", () => expect(calculateMonthlyTotal("1000","external")).toBe(100)); });
